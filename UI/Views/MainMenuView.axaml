<UserControl
	xmlns="https://github.com/avaloniaui"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:Nexen.ViewModels"
	xmlns:c="using:Nexen.Controls"
	xmlns:cfg="using:Nexen.Config"
	xmlns:l="using:Nexen.Localization"
	mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="650"
	VerticalAlignment="Top"
	x:DataType="vm:MainMenuViewModel"
	x:Class="Nexen.Views.MainMenuView"
>
	<Design.DataContext>
		<vm:MainMenuViewModel />
	</Design.DataContext>

	<DockPanel Background="{DynamicResource NexenMenuBackground}">
		<c:NexenMenu Name="LanguageMenu" DockPanel.Dock="Right">
			<MenuItem Header="🌐" ToolTip.Tip="{l:Translate mnuLanguageSelector}">
				<MenuItem Name="mnuLanguageEnglish" Header="{l:Translate mnuLanguageEnglish}" ToggleType="CheckBox" Click="mnuLanguageEnglish_Click" />
				<MenuItem Name="mnuLanguageSpanish" Header="{l:Translate mnuLanguageSpanish}" ToggleType="CheckBox" Click="mnuLanguageSpanish_Click" />
				<MenuItem Name="mnuLanguageJapanese" Header="{l:Translate mnuLanguageJapanese}" ToggleType="CheckBox" Click="mnuLanguageJapanese_Click" />
			</MenuItem>
		</c:NexenMenu>

		<c:NexenMenu Name="ActionMenu" DockPanel.Dock="Left">
			<MenuItem Header="{l:Translate mnuFile}" ItemsSource="{Binding FileMenuItems}" />
			<MenuItem Header="{l:Translate mnuGame}" ItemsSource="{Binding GameMenuItems}" />
			<MenuItem Header="{l:Translate mnuOptions}" ItemsSource="{Binding OptionsMenuItems}" />
			<MenuItem Header="{l:Translate mnuTools}" ItemsSource="{Binding ToolsMenuItems}" SubmenuOpened="mnuTools_Opened" />
			<MenuItem Header="{l:Translate mnuDebug}" ItemsSource="{Binding DebugMenuItems}" />
			<MenuItem Header="{l:Translate mnuHelp}" ItemsSource="{Binding HelpMenuItems}" />
		</c:NexenMenu>
		<Panel Name="MenuPanel" />
	</DockPanel>
</UserControl>
